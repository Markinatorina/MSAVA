<Page x:Class="MSAVA_App.Presentation.Welcome.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MSAVA_App.Presentation"
      xmlns:uen="using:Uno.Extensions.Navigation.UI"
      xmlns:utu="using:Uno.Toolkit.UI"
      NavigationCacheMode="Required"
      Background="{ThemeResource BackgroundBrush}">
    <ScrollViewer IsTabStop="True">
        <Grid utu:SafeArea.Insets="VisibleBounds">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Spacing="16">
                <TextBlock Margin="0,0,0,8" FontSize="16" HorizontalAlignment="Center" 
                           Text="{Binding WelcomeText}" />
                <Button Content="Open File Management"
                AutomationProperties.AutomationId="FileManagementButton"
                Command="{Binding GoToFiles}" />
                <Button Content="Logout"
                Command="{Binding Logout}" />
            </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
